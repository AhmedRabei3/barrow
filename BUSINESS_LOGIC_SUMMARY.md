# تحليل منطق الأعمال الأساسي لمشروع Barrow (منصة الدليل الآمن)

## 1. نظرة عامة على البنية

- يعتمد المشروع على Next.js (واجهة ويب)، Prisma (ORM)، PostgreSQL (قاعدة بيانات)، Supabase، Firebase، NextAuth.
- جميع العمليات الحساسة (سحب أرباح، إدارة الحساب، إلخ) تمر عبر تحقق الجلسة (auth) وتحقق الصلاحيات.

## 2. نموذج البيانات (Prisma)

- **المستخدم (User):**
  - يحتوي على خصائص مثل: id، الاسم، البريد، الرصيد، حالة التفعيل، صلاحيات (isAdmin/isOwner)، تاريخ التفعيل.
- **طلبات السحب ShamCash:**
  - نموذج ShamCashManualWithdrawal: يربط المستخدم بمبلغ، رقم محفظة، حالة الطلب (PENDING_ADMIN, VERIFYING, ...)، ملاحظات، إلخ.
  - لا يمكن للمستخدم إرسال أكثر من طلب سحب قيد المراجعة في نفس الوقت.
- **الإشعارات:**
  - كل عملية سحب أو حدث مهم يتم إشعار المستخدم والإدارة به عبر جدول notifications.
- **المحادثات والرسائل:**
  - نظام محادثة متكامل (ChatConversation, ChatMessage, ChatUnread) مرتبط بالإعلانات والمستخدمين.

## 3. منطق سحب الأرباح (ShamCash Withdraw)

- **التحقق:**
  - يجب أن يكون المستخدم مسجلاً وفعّالاً (أو مالك/owner).
  - يجب ألا يكون لديه طلب سحب قيد المراجعة.
  - يجب أن يكون الرصيد كافياً.
- **العملية:**
  - يتم إنشاء سجل جديد في ShamCashManualWithdrawal بالحالة PENDING_ADMIN.
  - إشعار المستخدم والإدارة بوجود طلب جديد.
  - الطلب ينتظر مراجعة الإدارة (لا يتم التنفيذ تلقائياً).

## 4. القيود والسياسات

- **معدل الطلبات:**
  - يوجد Rate Limiting على عمليات السحب (10 طلبات/دقيقة).
- **التحقق من صحة البيانات:**
  - رقم المحفظة والمبلغ إلزاميان.
  - المبلغ يجب أن يكون > 0 وأقل من أو يساوي الرصيد.
- **الإشعارات:**
  - كل طلب سحب يولد إشعاراً للمستخدم ولكل مدير/مالك.

## 5. ملاحظات إضافية

- جميع الرسائل تدعم العربية والإنجليزية.
- يوجد نظام Referral (دعوات) مرتبط بالمستخدمين والمكافآت.
- كل العمليات المهمة تُسجل زمنياً وتخضع للمراجعة اليدوية عند الحاجة.

---

هذا الملخص يغطي منطق الأعمال الأساسي للعمليات المالية، المستخدمين، المحادثات، والإشعارات. يمكن التوسع لاحقاً في تفاصيل الإعلانات، الطلبات، أو أي وحدة أخرى حسب الحاجة.
